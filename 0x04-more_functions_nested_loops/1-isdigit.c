#include "main.h"
#include <stdio.h>

/*
 * _isdigit - checks for digit 0 through 9
 * @c: character checked
 * Return: 1 if c is a digit, 0 otherwise
 */

int _isdigit(int c)
{
return (c >= 0 && c <= 9);
}
